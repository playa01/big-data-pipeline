# Data

`train.csv` is the local dataset used for the Hadoop/Hive coursework pipeline. It contains company registration, ownership, industry, financial, and risk-related fields.

The file is about 30 MB, which is acceptable for local review but may still be better stored with Git LFS or replaced by a smaller sample before publishing publicly.

