# Data

`train.csv` is the local dataset used for the Hadoop/Hive coursework pipeline. It contains company registration, ownership, industry, financial, and risk-related fields.

The file is about 30 MB. It is kept here so the coursework queries have context, but a cleaner long-term version of the repository should replace it with a smaller sample or move the full file to Git LFS.
